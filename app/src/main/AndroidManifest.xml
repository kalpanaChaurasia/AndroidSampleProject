<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.codeguy.myallcomponent">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.recyclerviewdemo.RecyclerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.UserActivity" />

        <activity android:name=".activity.launchmode.ActivityB"  android:launchMode="singleInstance"/>
        <activity android:name=".activity.launchmode.ActivityC" />
        <activity
            android:name=".activity.launchmode.ActivityD"
            android:launchMode="singleTop" />

    </application>


    <uses-permission android:name="android.permission.INTERNET" />

</manifest>